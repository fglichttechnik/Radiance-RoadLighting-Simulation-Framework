import Target as modulTarget
import ViewPoint as modulViewPoint

class TargetParameters:
    def __init__( self ):
        self.viewPoint = modulViewPoint.ViewPoint()
        self.target = modulTarget.Target()